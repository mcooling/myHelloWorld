{
  "type": "object",
  "id": "#/SecurityDomain",
  "properties": {
    "id": {
      "type": "integer",
      "minimum": 1,
      "maximum": 32767
    },
    "name": {
      "type": "string",
      "pattern": "^[A-Z][a-z0-9]+([A-Z][a-z0-9]*)*"
    },
    "label": {
      "type": "string",
      "pattern": "^[A-Z]+([A-Za-z0-9:\\-\\s]*)*"
    }
  },
  "required": [
    "id",
    "name"
  ],
  "additionalProperties": false
}